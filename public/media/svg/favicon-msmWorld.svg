<svg>
    <style>
        path {
            fill: black;
        }

        @media (prefers-color-scheme: dark) {
            path {
                fill: white;
            }

            :root {
                filter: brightness(2);
                filter: invert(100%);
            }
        }
    </style>

    <!-- more stuff -->

</svg>